load("//tools:golden_test.bzl", "golden_test")

golden_test(name = "identity")

golden_test(name = "max")

golden_test(name = "ttl_decrement")

golden_test(name = "vss_actions")

golden_test(name = "vss_types")
